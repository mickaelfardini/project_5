{% extends "/backoffice/base.html.twig" %}

{% block title %}Admin{% endblock %}

{% block content %} 
     <!-- Content Row  -->
    <div class="row">
      <!-- Content Column -->
      <div class="col-lg-12 mb-8">
        <!-- Posts List -->
        <div class="card shadow mb-4">
          <div class="card-header py-3 bg-gray">
            <h6 class="m-0 font-weight-bold text-black">Liste des articles</h6>
          </div>
          <div class="card-body">
            <table class="table table-striped table-dark text-center" aria-describedby="liste des articles">
              <thead>
                <tr>
                  <th scope="col">Auteur</th>
                  <th scope="col">Titre</th>
                  <th scope="col">Chapô</th>
                  <th scope="col">Date de création</th>
                  <th scope="col">Action</th>
                </tr>
              </thead>
              <tbody>
                 {% for post in posts %}
                  <tr> 
                      <td>{{ post.username}}</td>
                      <td>{{ post.title}}</td>
                      <td>{{ post.chapo }}</td>
                      <td>{{ post.createdAt | date('d/m/Y')}}</td>
                  </tr>
                  {% endfor %}
              </tbody>
            </table>
              <a class="btn btn-sm btn-primary shadow-sm" data-toggle="modal" href="">Modifier</a>
                      <a class="btn btn-sm btn-danger shadow-sm" data-toggle="modal" href="">Supprimer</a>
                      </td>

          </div>
        </div>
      </div> 
    </div>
      <td>

      
        

{% endblock %}

